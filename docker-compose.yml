services:
  web:
    build: .
    ports:
      - "80:3000"
    volumes:
      # data persists across container restarts; posts and vacancies survive redeploys
      - ./src/data:/app/src/data
    restart: unless-stopped
    environment:
      - NODE_ENV=production
      - ADMIN_PASS=${ADMIN_PASS}
